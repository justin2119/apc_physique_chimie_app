import 'package:flutter/material.dart';
class ProblemSituationsPage extends StatelessWidget {
  const ProblemSituationsPage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Situations-problèmes')));
}
