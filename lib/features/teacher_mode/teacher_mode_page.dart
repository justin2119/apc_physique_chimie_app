import 'package:flutter/material.dart';
class TeacherModePage extends StatelessWidget {
  const TeacherModePage({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Mode enseignant')));
}
